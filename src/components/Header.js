import React from 'react'
import './Header.css';

const Header = (props) => {

    return (
        <div className="header">
            <span className="logo">Gaming Analytics</span>
        </div>
    );
}

export default Header;